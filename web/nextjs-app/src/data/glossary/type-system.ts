// src/data/glossary/type-system.ts
import type { GlossaryTerm } from "@/types";

export const TYPE_SYSTEM_TERMS: GlossaryTerm[] = [
  {
    slug: "type-hint",
    term: "Type Hint",
    category: "type-system",
    categoryLabel: "Type System",
    difficulty: "beginner",
    definition: "Explicit annotations added to variables, function parameters, and return values indicating their expected types.",
    explanation: "Introduced in PEP 484. Python ignores type hints at runtime (they do not affect execution speed or cause crashes), but static analysis tools (like mypy and IDEs) use them for verification.",
    syntax: "def greet(name: str, count: int = 1) -> list[str]:\n    return [f\"Hi {name}\"] * count",
    example: "def multiply(x: float, y: float) -> float:\n    return x * y\n\nprint(multiply(2.5, 4.0))",
    output: "10.0",
    whyItMatters: "Drastically improves code readability, enables IDE autocomplete, and catches bugs before code ever runs in production.",
    relatedTerms: ["generic", "optional", "protocol", "mypy"],
    relatedLessons: [{ title: "Type Annotations in Python", phaseSlug: "10-advanced-python", lessonSlug: "type-hints" }],
    tags: ["typing", "type-hints", "mypy", "modern-python"],
  },
  {
    slug: "generic",
    term: "Generic / TypeVar",
    category: "type-system",
    categoryLabel: "Type System",
    difficulty: "advanced",
    definition: "A mechanism that allows functions, classes, and data structures to operate over different types while preserving full static type safety.",
    explanation: "`TypeVar` represents a parameterized placeholder type. Python 3.12+ supports native generic syntax with `def first[T](items: list[T]) -> T:`.",
    example: "from typing import TypeVar, Sequence\n\nT = TypeVar('T')\n\ndef get_first(items: Sequence[T]) -> T:\n    return items[0]\n\nprint(get_first([10, 20, 30]))\nprint(get_first([\"a\", \"b\"]))",
    output: "10\na",
    whyItMatters: "Enables building flexible, reusable collections and algorithms without sacrificing type checking accuracy.",
    relatedTerms: ["type-hint", "protocol", "typing"],
    relatedLessons: [{ title: "Generics in Python", phaseSlug: "10-advanced-python", lessonSlug: "generics" }],
    tags: ["typing", "generics", "advanced"],
  },
  {
    slug: "optional",
    term: "Optional / Union",
    category: "type-system",
    categoryLabel: "Type System",
    difficulty: "beginner",
    definition: "A type annotation specifying that a value can either be of a specific type or `None` (in modern Python 3.10+, written as `T | None`).",
    explanation: "`Optional[str]` is an alias for `Union[str, None]` or `str | None`.",
    example: "def find_user(user_id: int) -> str | None:\n    if user_id == 1:\n        return \"Alice\"\n    return None\n\nprint(find_user(1))\nprint(find_user(99))",
    output: "Alice\nNone",
    whyItMatters: "Explicitly communicates and enforces handling of nullable values, preventing `AttributeError: 'NoneType' object has no attribute` bugs.",
    relatedTerms: ["none", "type-hint", "union"],
    relatedLessons: [{ title: "Type Annotations", phaseSlug: "10-advanced-python", lessonSlug: "type-hints" }],
    tags: ["typing", "nullable", "modern-python"],
  },
  {
    slug: "protocol",
    term: "Protocol",
    category: "type-system",
    categoryLabel: "Type System",
    difficulty: "advanced",
    definition: "A static structural subtyping mechanism (static duck typing) defined via `typing.Protocol` that checks whether an object has required methods without explicit inheritance.",
    explanation: "If class `C` implements all methods declared in Protocol `P`, `C` is automatically considered a valid subtype of `P` by static type checkers.",
    example: "from typing import Protocol\n\nclass Renderable(Protocol):\n    def render(self) -> str: ...\n\nclass Button:\n    def render(self) -> str:\n        return \"<button />\"\n\ndef draw(widget: Renderable):\n    print(widget.render())\n\ndraw(Button())",
    output: "<button />",
    whyItMatters: "Enables type-safe duck typing without tightly coupling components to concrete base classes.",
    relatedTerms: ["duck-typing", "abstract-base-class", "type-hint"],
    relatedLessons: [{ title: "Protocols & Structural Typing", phaseSlug: "10-advanced-python", lessonSlug: "protocols" }],
    tags: ["typing", "protocols", "architecture"],
  },
  {
    slug: "mypy",
    term: "mypy",
    category: "type-system",
    categoryLabel: "Type System",
    difficulty: "intermediate",
    definition: "The standard static type checker for Python that analyzes type annotations at build/CI time to catch type errors before execution.",
    explanation: "Acts as a compile-time verification step for Python projects without altering Python's dynamic runtime execution.",
    example: "# Run in terminal:\n# mypy src/\n# Success: no issues found in 12 source files",
    output: "Success: no issues found",
    whyItMatters: "Standard tool in CI/CD pipelines to ensure enterprise codebase stability and refactoring safety.",
    relatedTerms: ["type-hint", "static-typing", "linter"],
    relatedLessons: [{ title: "Static Type Checking with mypy", phaseSlug: "13-testing", lessonSlug: "mypy" }],
    tags: ["tooling", "typing", "ci-cd"],
  },
];

