// src/data/glossary/concurrency.ts
import type { GlossaryTerm } from "@/types";

export const CONCURRENCY_TERMS: GlossaryTerm[] = [
  {
    slug: "gil",
    term: "GIL (Global Interpreter Lock)",
    category: "concurrency",
    categoryLabel: "Concurrency & GIL",
    difficulty: "advanced",
    definition: "A mutex mechanism used by CPython to ensure that only one native OS thread executes Python bytecode at any given moment.",
    explanation: "Designed to make CPython's memory management and C-extensions thread-safe. As a result, standard Python threads cannot achieve true CPU-bound parallelism on multi-core systems (use multiprocessing for CPU work, threading/asyncio for I/O).",
    example: "import sys\n# In Python 3.13+, free-threaded builds can disable the GIL\nprint(hasattr(sys, \"_is_gil_enabled\"))",
    output: "True",
    whyItMatters: "Dictates Python's concurrency architecture: use `multiprocessing` for CPU-bound tasks, and `asyncio`/`threading` for I/O-bound tasks.",
    comparison: {
      title: "Threading vs Multiprocessing vs Asyncio",
      differences: [
        { name: "Threading", description: "Shared memory, preemptive OS threads; constrained by GIL (ideal for I/O-bound blocking calls)." },
        { name: "Multiprocessing", description: "Separate processes, independent Python runtimes/GILs; true CPU multi-core parallelism." },
        { name: "Asyncio", description: "Single-thread, cooperative single-event-loop concurrency; extremely high concurrency for non-blocking network I/O." },
      ],
    },
    relatedTerms: ["threading", "multiprocessing", "asyncio"],
    relatedLessons: [{ title: "The GIL & Concurrency Models", phaseSlug: "19-asynchronous-python", lessonSlug: "gil" }],
    tags: ["gil", "concurrency", "cpython", "internals"],
  },
  {
    slug: "threading",
    term: "Threading",
    category: "concurrency",
    categoryLabel: "Concurrency & GIL",
    difficulty: "intermediate",
    definition: "A concurrency model using the `threading` module where multiple lightweight OS threads run concurrently within the same process memory space.",
    explanation: "Great for I/O-bound tasks (downloading files, waiting on network calls) where the GIL is released during system calls.",
    example: "import threading\n\ndef worker(num):\n    print(f\"Thread {num}\")\n\nt = threading.Thread(target=worker, args=(1,))\nt.start()\nt.join()",
    output: "Thread 1",
    whyItMatters: "Enables background workers, GUI responsiveness, and concurrent network requests without process spawning overhead.",
    relatedTerms: ["gil", "multiprocessing", "lock"],
    relatedLessons: [{ title: "Threading in Python", phaseSlug: "19-asynchronous-python", lessonSlug: "threading" }],
    tags: ["concurrency", "threads", "stdlib"],
  },
  {
    slug: "multiprocessing",
    term: "Multiprocessing",
    category: "concurrency",
    categoryLabel: "Concurrency & GIL",
    difficulty: "intermediate",
    definition: "A parallelism model using the `multiprocessing` module that spawns separate OS processes, each with its own memory space and Python interpreter.",
    explanation: "Completely bypasses the GIL to utilize 100% of all available CPU cores for heavy mathematical calculations or data processing.",
    example: "from multiprocessing import Pool\n\ndef square(n):\n    return n * n\n\nif __name__ == \"__main__\":\n    with Pool(2) as p:\n        print(p.map(square, [1, 2, 3]))",
    output: "[1, 4, 9]",
    whyItMatters: "The standard way to achieve true multi-core CPU parallelism in Python.",
    relatedTerms: ["gil", "threading", "process-pool"],
    relatedLessons: [{ title: "Multiprocessing & Parallelism", phaseSlug: "19-asynchronous-python", lessonSlug: "multiprocessing" }],
    tags: ["concurrency", "parallelism", "multiprocessing"],
  },
  {
    slug: "race-condition",
    term: "Race Condition",
    category: "concurrency",
    categoryLabel: "Concurrency & GIL",
    difficulty: "intermediate",
    definition: "A concurrency bug where the correctness of a program depends on the unpredictable execution timing or interleaving of concurrent threads.",
    explanation: "Occurs when two threads read and modify shared mutable state without synchronization (e.g. `counter += 1`).",
    example: "import threading\n\nlock = threading.Lock()\ncounter = 0\n\ndef increment():\n    global counter\n    with lock:  # Prevents race condition\n        counter += 1",
    output: "",
    whyItMatters: "Understanding race conditions is critical for writing correct, thread-safe concurrent programs using locks and queues.",
    relatedTerms: ["lock", "deadlock", "threading"],
    relatedLessons: [{ title: "Thread Synchronization & Locks", phaseSlug: "19-asynchronous-python", lessonSlug: "locks" }],
    tags: ["concurrency", "safety", "bugs"],
  },
  {
    slug: "lock",
    term: "Lock (Mutex)",
    category: "concurrency",
    categoryLabel: "Concurrency & GIL",
    difficulty: "intermediate",
    definition: "A synchronization primitive that allows only one thread at a time to enter a critical section of code.",
    explanation: "Used with a `with lock:` context manager to ensure mutual exclusion when modifying shared state.",
    example: "import threading\n\nlock = threading.Lock()\nwith lock:\n    print(\"Safely inside critical section\")",
    output: "Safely inside critical section",
    whyItMatters: "Prevents data corruption and race conditions across multi-threaded applications.",
    relatedTerms: ["race-condition", "deadlock", "context-manager"],
    relatedLessons: [{ title: "Locks & Semaphores", phaseSlug: "19-asynchronous-python", lessonSlug: "locks" }],
    tags: ["concurrency", "synchronization", "safety"],
  },
];

