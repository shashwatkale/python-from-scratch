// src/data/glossary/async-python.ts
import type { GlossaryTerm } from "@/types";

export const ASYNC_PYTHON_TERMS: GlossaryTerm[] = [
  {
    slug: "async-await",
    term: "async / await",
    category: "async",
    categoryLabel: "Asynchronous Python",
    difficulty: "intermediate",
    definition: "Syntax for defining and executing cooperative asynchronous coroutines in Python.",
    explanation: "`async def` defines a coroutine function; `await` yields control back to the event loop while waiting for an asynchronous operation to complete.",
    syntax: "async def fetch_data():\n    response = await http_client.get(url)\n    return response",
    example: "import asyncio\n\nasync def greet():\n    await asyncio.sleep(0.01)\n    return \"Hello Async!\"\n\nprint(asyncio.run(greet()))",
    output: "Hello Async!",
    whyItMatters: "The modern foundation for high-performance Python web frameworks (FastAPI, aiohttp, Starlette) handling tens of thousands of concurrent connections.",
    relatedTerms: ["coroutine", "event-loop", "asyncio", "task"],
    relatedLessons: [{ title: "async & await Fundamentals", phaseSlug: "19-asynchronous-python", lessonSlug: "async-await" }],
    tags: ["async", "concurrency", "keywords", "modern-python"],
  },
  {
    slug: "coroutine",
    term: "Coroutine",
    category: "async",
    categoryLabel: "Asynchronous Python",
    difficulty: "intermediate",
    definition: "A specialized function that can pause execution (via `await`), yield control back to an event loop, and resume execution later.",
    explanation: "Calling an `async def` function does not execute its body immediately; it returns a coroutine object that must be scheduled on an event loop or awaited.",
    example: "import asyncio\n\nasync def task():\n    return 42\n\ncoro = task()\nprint(type(coro))\nprint(asyncio.run(coro))",
    output: "<class 'coroutine'>\n42",
    whyItMatters: "Coroutines enable non-blocking cooperative multitasking in a single OS thread with minimal memory overhead.",
    relatedTerms: ["async-await", "event-loop", "task"],
    relatedLessons: [{ title: "Coroutines in Python", phaseSlug: "19-asynchronous-python", lessonSlug: "coroutines" }],
    tags: ["async", "coroutines"],
  },
  {
    slug: "event-loop",
    term: "Event Loop",
    category: "async",
    categoryLabel: "Asynchronous Python",
    difficulty: "intermediate",
    definition: "The core engine of `asyncio` that manages and schedules the execution of coroutines, I/O callbacks, and asynchronous tasks.",
    explanation: "Runs in a continuous loop: monitoring socket/file I/O readiness, waking up paused coroutines, and dispatching execution cooperatively.",
    example: "import asyncio\n\nasync def main():\n    loop = asyncio.get_running_loop()\n    return loop.is_running()\n\nprint(asyncio.run(main()))",
    output: "True",
    whyItMatters: "Understanding the event loop prevents common pitfalls like executing blocking synchronous code inside async routes.",
    relatedTerms: ["async-await", "asyncio", "task"],
    relatedLessons: [{ title: "The asyncio Event Loop", phaseSlug: "19-asynchronous-python", lessonSlug: "event-loop" }],
    tags: ["async", "internals", "event-loop"],
  },
  {
    slug: "asyncio-gather",
    term: "asyncio.gather()",
    category: "async",
    categoryLabel: "Asynchronous Python",
    difficulty: "intermediate",
    definition: "A function that runs multiple awaitable objects or tasks concurrently and returns an aggregated list of their results in order.",
    explanation: "Allows executing multiple network requests, database queries, or background jobs in parallel without blocking sequentially.",
    example: "import asyncio\n\nasync def fetch(n):\n    await asyncio.sleep(0.01)\n    return n * 10\n\nasync def main():\n    results = await asyncio.gather(fetch(1), fetch(2), fetch(3))\n    print(results)\n\nasyncio.run(main())",
    output: "[10, 20, 30]",
    whyItMatters: "Standard idiom for fan-out / fan-in concurrent I/O operations.",
    relatedTerms: ["async-await", "coroutine", "task"],
    relatedLessons: [{ title: "Concurrent Tasks with gather", phaseSlug: "19-asynchronous-python", lessonSlug: "asyncio-gather" }],
    tags: ["async", "concurrency", "stdlib"],
  },
];

