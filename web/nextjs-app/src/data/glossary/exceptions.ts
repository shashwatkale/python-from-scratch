// src/data/glossary/exceptions.ts
import type { GlossaryTerm } from "@/types";

export const EXCEPTIONS_TERMS: GlossaryTerm[] = [
  {
    slug: "exception",
    term: "Exception",
    category: "exceptions",
    categoryLabel: "Exceptions & Error Handling",
    difficulty: "beginner",
    definition: "An event detected during execution that disrupts the normal flow of program instructions.",
    explanation: "Exceptions in Python are objects inheriting from `BaseException` (typically `Exception`) that propagate up the call stack until caught by a `try...except` block.",
    example: "try:\n    num = int(\"abc\")\nexcept ValueError as e:\n    print(\"Handled error:\", e)",
    output: "Handled error: invalid literal for int() with base 10: 'abc'",
    whyItMatters: "Enables robust, graceful error handling and fault tolerance without program crashes.",
    relatedTerms: ["try-except", "raise", "custom-exception"],
    relatedLessons: [{ title: "Exception Handling Basics", phaseSlug: "07-exceptions", lessonSlug: "try-except" }],
    tags: ["exceptions", "error-handling", "basics"],
  },
  {
    slug: "try-except",
    term: "try / except / else / finally",
    category: "exceptions",
    categoryLabel: "Exceptions & Error Handling",
    difficulty: "beginner",
    definition: "The primary control-flow construct for intercepting and handling runtime exceptions.",
    explanation: "`try` tests a block; `except` catches specific errors; `else` runs only if NO error occurred; `finally` ALWAYS runs (guaranteed cleanup).",
    syntax: "try:\n    # code\nexcept SomeError as e:\n    # handle\nelse:\n    # success only\nfinally:\n    # always runs",
    example: "try:\n    result = 10 / 2\nexcept ZeroDivisionError:\n    print(\"Cannot divide by zero\")\nelse:\n    print(f\"Result is {result}\")\nfinally:\n    print(\"Cleanup done\")",
    output: "Result is 5.0\nCleanup done",
    whyItMatters: "Provides complete control over recovery logic, resource release, and structured failure reporting.",
    relatedTerms: ["exception", "raise", "context-manager"],
    relatedLessons: [{ title: "try/except/else/finally", phaseSlug: "07-exceptions", lessonSlug: "try-except-blocks" }],
    tags: ["exceptions", "syntax", "control-flow"],
  },
  {
    slug: "raise",
    term: "raise",
    category: "exceptions",
    categoryLabel: "Exceptions & Error Handling",
    difficulty: "beginner",
    definition: "A statement used to explicitly trigger an exception and interrupt normal execution.",
    explanation: "Can raise built-in exceptions or custom subclassed exceptions. Bare `raise` inside an `except` block re-raises the current active exception.",
    example: "def validate_age(age: int):\n    if age < 0:\n        raise ValueError(\"Age cannot be negative\")\n    return age\n\ntry:\n    validate_age(-5)\nexcept ValueError as err:\n    print(err)",
    output: "Age cannot be negative",
    whyItMatters: "Enforces input validation and business logic constraints by failing fast with clear error messages.",
    relatedTerms: ["exception", "custom-exception", "assert"],
    relatedLessons: [{ title: "Raising Exceptions", phaseSlug: "07-exceptions", lessonSlug: "raising-exceptions" }],
    tags: ["exceptions", "validation", "syntax"],
  },
  {
    slug: "custom-exception",
    term: "Custom Exception",
    category: "exceptions",
    categoryLabel: "Exceptions & Error Handling",
    difficulty: "intermediate",
    definition: "A user-defined error class created by subclassing Python's built-in `Exception` class.",
    explanation: "Allows domain-specific error categorization so API callers can catch precise operational failures.",
    example: "class UserNotFoundError(Exception):\n    \"\"\"Raised when user ID does not exist.\"\"\"\n    pass\n\ndef find_user(uid):\n    if uid != 1:\n        raise UserNotFoundError(f\"User {uid} missing\")\n\ntry:\n    find_user(99)\nexcept UserNotFoundError as e:\n    print(e)",
    output: "User 99 missing",
    whyItMatters: "Crucial for writing clean library APIs, backend services, and domain-driven design.",
    relatedTerms: ["exception", "inheritance", "raise"],
    relatedLessons: [{ title: "Custom Exceptions", phaseSlug: "07-exceptions", lessonSlug: "custom-exceptions" }],
    tags: ["exceptions", "oop", "architecture"],
  },
  {
    slug: "type-error",
    term: "TypeError",
    category: "exceptions",
    categoryLabel: "Exceptions & Error Handling",
    difficulty: "beginner",
    definition: "Raised when an operation or function is applied to an object of an inappropriate or incompatible data type.",
    explanation: "Examples: adding a string to an integer, indexing a non-subscriptable object, or passing the wrong number of arguments.",
    example: "try:\n    len(12345)  # Integers do not have length\nexcept TypeError as e:\n    print(\"TypeError:\", e)",
    output: "TypeError: object of type 'int' has no len()",
    whyItMatters: "One of the most frequent errors in dynamically typed Python code.",
    relatedTerms: ["value-error", "strong-typing", "exception"],
    relatedLessons: [{ title: "Common Built-in Exceptions", phaseSlug: "07-exceptions", lessonSlug: "builtin-exceptions" }],
    tags: ["exceptions", "types"],
  },
  {
    slug: "value-error",
    term: "ValueError",
    category: "exceptions",
    categoryLabel: "Exceptions & Error Handling",
    difficulty: "beginner",
    definition: "Raised when an operation receives an argument that has the right type but an inappropriate value.",
    explanation: "For example, `int('42')` works because string `'42'` can be parsed, but `int('hello')` raises `ValueError`.",
    example: "try:\n    val = int(\"not_a_number\")\nexcept ValueError as e:\n    print(\"ValueError:\", e)",
    output: "ValueError: invalid literal for int() with base 10: 'not_a_number'",
    whyItMatters: "Standard exception for invalid domain inputs, parsing errors, and malformed parameters.",
    relatedTerms: ["type-error", "raise", "exception"],
    relatedLessons: [{ title: "Common Built-in Exceptions", phaseSlug: "07-exceptions", lessonSlug: "builtin-exceptions" }],
    tags: ["exceptions", "validation"],
  },
];

