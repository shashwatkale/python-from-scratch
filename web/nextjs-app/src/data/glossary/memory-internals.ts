// src/data/glossary/memory-internals.ts
import type { GlossaryTerm } from "@/types";

export const MEMORY_INTERNALS_TERMS: GlossaryTerm[] = [
  {
    slug: "reference-counting",
    term: "Reference Counting",
    category: "memory-internals",
    categoryLabel: "Memory & Internals",
    difficulty: "advanced",
    definition: "CPython's primary memory management mechanism where every object maintains a count of references pointing to it; when the count hits zero, the memory is deallocated immediately.",
    explanation: "Managed in CPython via the `ob_refcnt` field in `PyObject`. Accessible via `sys.getrefcount()`.",
    example: "import sys\n\nx = []\nprint(sys.getrefcount(x))  # 2 (x + argument to getrefcount)",
    output: "2",
    whyItMatters: "Provides deterministic, instantaneous resource deallocation as soon as objects go out of scope.",
    relatedTerms: ["garbage-collection", "circular-reference", "weak-reference"],
    relatedLessons: [{ title: "Memory Management & Internals", phaseSlug: "10-advanced-python", lessonSlug: "memory-management" }],
    tags: ["memory", "internals", "cpython"],
  },
  {
    slug: "garbage-collection",
    term: "Garbage Collection (GC)",
    category: "memory-internals",
    categoryLabel: "Memory & Internals",
    difficulty: "advanced",
    definition: "A cyclic garbage collector in CPython that runs periodically to detect and clean up circular reference cycles that reference counting alone cannot reclaim.",
    explanation: "Categorizes objects into three generations (0, 1, 2) based on how many collection cycles they survive. Managed via the `gc` module.",
    example: "import gc\n\n# Force a collection cycle\ncollected = gc.collect()\nprint(f\"Reclaimed objects: {collected >= 0}\")",
    output: "Reclaimed objects: True",
    whyItMatters: "Prevents memory leaks in complex applications containing self-referencing nodes or circular graphs.",
    relatedTerms: ["reference-counting", "circular-reference"],
    relatedLessons: [{ title: "Memory Management & GC", phaseSlug: "10-advanced-python", lessonSlug: "memory-management" }],
    tags: ["memory", "gc", "internals"],
  },
  {
    slug: "object-identity",
    term: "Object Identity & id()",
    category: "memory-internals",
    categoryLabel: "Memory & Internals",
    difficulty: "intermediate",
    definition: "A unique integer guaranteed to be unique and constant for an object during its lifetime. In CPython, `id()` returns the object's actual memory address.",
    explanation: "Tested using the `is` keyword (`a is b`), whereas equality is tested using `==` (`a == b`).",
    example: "a = [1, 2]\nb = [1, 2]\nprint(a == b)  # True (equal values)\nprint(a is b)  # False (distinct memory objects)",
    output: "True\nFalse",
    whyItMatters: "Crucial for understanding identity vs equality and avoiding bugs with singleton checks like `x is None`.",
    comparison: {
      title: "is vs ==",
      differences: [
        { name: "is", description: "Checks identity (same memory address, `id(a) == id(b)`)." },
        { name: "==", description: "Checks equality of values via `__eq__()`." },
      ],
    },
    relatedTerms: ["reference", "none", "interning"],
    relatedLessons: [{ title: "Identity vs Equality", phaseSlug: "01-python-basics", lessonSlug: "identity-equality" }],
    tags: ["memory", "identity", "basics"],
  },
  {
    slug: "interning",
    term: "String / Integer Interning",
    category: "memory-internals",
    categoryLabel: "Memory & Internals",
    difficulty: "advanced",
    definition: "An optimization in CPython where single copies of immutable objects (small integers -5 to 256, identifier-like strings) are cached and reused globally.",
    explanation: "Speeds up dictionary lookups by allowing pointer comparison instead of character-by-character string comparison.",
    example: "a = 250\nb = 250\nprint(a is b)  # True due to small integer interning",
    output: "True",
    whyItMatters: "Saves memory and accelerates identifier resolution throughout the Python interpreter.",
    relatedTerms: ["object-identity", "immutable", "cpython"],
    relatedLessons: [{ title: "CPython Optimizations", phaseSlug: "10-advanced-python", lessonSlug: "cpython-optimizations" }],
    tags: ["memory", "internals", "optimization"],
  },
];

